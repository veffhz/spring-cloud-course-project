# Demo web application with Spring & Vue.js

#### used
java, gradle, mysql, node, npm.

#### run MySql in docker
* `docker-compose up`
   
based on the course "Vue JS + Spring Boot Microservices and Spring Cloud"

